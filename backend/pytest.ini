[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
norecursedirs = .venv env venv __pycache__ .. ../tests
addopts = 
    -v
    --strict-markers
    --tb=short
    --cov=backend
    --cov-report=term-missing
    --cov-report=html
markers =
    asyncio: mark test as async
    unit: mark test as unit test
    integration: mark test as integration test
    performance: mark performance-focused tests
    reliability: mark reliability-focused tests
    security: mark security-focused tests
    failure: mark test as failure scenario test
    concurrency: mark test as concurrency test
    regression: mark test as regression test
    production_safety: mark test as production safety test
